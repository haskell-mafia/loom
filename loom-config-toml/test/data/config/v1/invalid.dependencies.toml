[loom]
  version = 1
  dependencies = ["missing.toml"]
  name = "invalid.dependencies"
  output = "dist"
  assetsPrefix = "assets"

[component]
  paths = ["components/*"]

[sass]
  paths = ["scss/*"]
